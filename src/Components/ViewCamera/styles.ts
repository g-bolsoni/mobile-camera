import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  buttonContainer: {
    alignSelf: "flex-end",
  },
  buttonRecord: {
    backgroundColor: "#fff",
    padding: 10,
    borderRadius: 8,
  },
  buttonText: {
    color: "#000",
  },
});
